export enum FieldType {
    TEXT = 'text',
    TEXTAREA = 'textarea',
    NUMBER = 'number',
    SELECT = 'select',
    DATE = 'date',
    FILE = 'file'
}
export enum DurationUnit {
    DAYS = 'days',
    HOURS = 'hours',
    MONTHS = 'months'
}



